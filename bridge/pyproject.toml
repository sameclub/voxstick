[project]
name = "voxstick-s3ai-bridge"
version = "0.3.0"
description = "Cross-platform VoxStick bridge for the S3AI Game device."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
voxstick-bridge = "vox_stick.__main__:main"

[tool.setuptools.package-data]
"vox_stick.paste" = ["*.ps1"]
